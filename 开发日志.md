# 📔 开发日志 - 视频排列拼接工具

---

## 2026年6月1日 · 周一 · 晴

### 下午 · 项目启动

今天阿瑞找到我，说要做一个视频拼接的工具。

他给我描述需求的时候，我一开始以为就是把几个视频拼在一起，心想这不就 ffmpeg 一行命令的事嘛。结果越聊越发现，根本不是那么回事——他们团队做护肤科普视频，每个视频被拆成了好几个部分（A、B、C、D），每个部分又有好几个版本（A1、A2、A3...），他需要的是把这些全部排列组合起来，生成成百上千条不同的拼接方案。

我心想好家伙，这是要做一个排列组合引擎啊。

### 下午 · 来回拉扯的需求确认

我和阿瑞来回确认了好几轮。

最开始他说文件命名是 `A1.mp4`、`A2.mp4` 这种，我问有没有下划线，他说没有。然后我说那文件夹怎么组织？他说剪辑人员会按日期建文件夹，比如 `input/20260601/`，所有视频直接扔里面，不分子文件夹。我一开始觉得这样有点乱，但他说这对剪辑人员效率更高——不用来回切文件夹，我就理解了。

然后是组数的问题。有时候是 ABC 三组，有时候是 ABCD 四组，甚至可能五组。我问他能不能固定，他说不行，得灵活。OK，那我就写成自动扫描首字母分组，几组都行。

排列模式他要两种：有序（固定 A→B→C→D 顺序）和无序（随机组序）。这个不难。

但有个关键需求——"均匀覆盖"。他说如果每组有 10 个视频，四组就是一万种排列，他不可能全生成，所以每次只生成一部分，但要保证每个子视频出现的次数尽量均衡。不能出现 A3 出现了 25 次而 A7 一次都没有的情况。

这个需求有意思，我花了不少时间设计这个算法。

### 下午 · 命令行版完成

我先写了一个命令行版 `video_joiner.py`。测试的时候用了 7 个不同分辨率、不同帧率的测试视频——640x480 的 24fps、1280x720 的 30fps、1920x1080 的 25fps，故意搞得不一样，就为了验证自动统一分辨率的功能。

全部通过了，FFmpeg 的 filter_complex 能自动缩放、加黑边、统一帧率，拼出来的视频看着还挺顺的。

### 下午 · 升级 GUI

阿瑞说他不想用命令行，想要一个真正的程序，双击就能用的那种。我想也是，给剪辑人员用的东西，总不能让人家打开终端输命令吧。

于是用 tkinter 写了一个图形界面。说实话 tkinter 丑是丑了点，但胜在 Python 自带，不用额外装东西。我加了文件夹选择、参数设置、分组预览、进度条、实时日志这些，基本够用了。

### 下午 · 路径与配置

阿瑞指定了三个位置：
- 脚本放 `D:\SMB\Vscodeproject\拼接视频\`
- 素材放 `D:\SMB\code_file\拼接视频\input\`
- 输出放 `D:\SMB\code_file\拼接视频\output\`

他说不希望东西堆在 C 盘，我把之前在工作区生成的测试文件全清理了，脚本都搬到了 D 盘。

加了一个「⚙ 设置」按钮，可以改默认路径，配置保存到 `config.json`，下次打开自动恢复。

### 下午 · 视频命名格式

这个确认了好几轮。最终定下来：
```
20260601_A1B1C1D1.mp4
│       └── 各组视频名直接拼接
└── 日期文件夹名
```

没有多余的下划线和分隔符，紧凑明了。

### 下午 · 第一次大重构（累积模式）

阿瑞提出了一个我没考虑到的场景：他可能需要多次运行工具，每次生成一部分排列，但不能和之前的重复。

于是我想到了"累积模式"——读取已有表格，排除已有的排列，新排列追加到末尾。有序和无序分别用不同的表格（`20260601有序.xlsx`、`20260601无序.xlsx`）。

我写完觉得挺优雅的，给阿瑞看。

### 下午 · 第二次大重构（简化）

阿瑞看完说："太复杂了。"

他提出了一个更简洁的方案。我仔细想了想，确实比我那个好：

- 「仅生成表格」：直接生成所有排列，有旧表格就覆盖
- 「开始拼接」：有表格就读表格继续拼未完成的，没表格就先生成再拼

这个设计简单明了。我之前的累积排重逻辑虽然能用，但对用户来说理解成本太高了。阿瑞一句话点醒了我："不如一次性生成所有排列组合。"

我加了几个保护措施：超过 500 种排列时弹窗确认、表格已存在时确认覆盖、帧率自动以第一个视频为准。

### 下午 · 关键修正

阿瑞又指出了一个我理解偏差。

我把「生成数量」去掉了，以为表格和视频都是全量生成。但阿瑞说：**表格**确实要全量生成（它是目录/索引），但**视频**只需要拼指定数量。比如表格里有一万种排列，他可能这次只想拼 100 条视频。

这个修正很重要。我重新加回了「拼接数量」输入框，逻辑变成：
- 表格：永远全量
- 视频：按指定数量取前 N 条

### 傍晚 · Bug 时间

阿瑞跑起来了，结果 10 条视频全部报错：
```
concat_videos() got an unexpected keyword argument 'res_w'
```

我一看，是参数名不匹配。`_encode_loop` 传 `res_w`，但 `concat_videos` 定义的是 `target_w`。重构的时候改了函数签名但没改调用处，低级错误。

修完又报：
```
the JSON object must be str, bytes or bytearray, not NoneType
```

这个更隐蔽——`subprocess.run` 在后台线程里用 `text=True` 模式时，`stdout` 可能因为 Windows 编码问题返回 `None`。改成二进制模式手动解码就好了。

两个 bug 都修完了，阿瑞那边跑通了。

### 傍晚 · 组别选择

阿瑞说文件夹里有 ABCD 四组，但他有时候只想组合 ABD 或 ABC。

我在预览区加了复选框，每组一个勾选框，还有「全选」「全不选」的快捷按钮。排列数量会根据选择实时更新。至少需要勾选两组才能开始。

这个功能改动不大，但很实用。

### 傍晚 · 关键词分组

这是今天最后一个需求，也是我觉得最有意思的一个。

阿瑞说他们的视频命名不一定是 `A1.mp4` 这种格式，可能是 `开头_产品介绍.mp4`、`痛点_干燥脱皮.mp4` 这种带语义的名字。他想用关键词来定义组别——比如 `A=开头` 就把所有文件名包含"开头"的视频归为 A 组。

我在文件夹设置区加了一个「组规则」输入框：
```
A=开头,B=痛点,C=结尾,D=效果
```

解析逻辑不复杂，但要考虑很多边界情况：关键词为空怎么办、一个文件匹配多个关键词怎么办（取第一个）、文件名里没有数字怎么办（序号默认为 0）。

留空的话就走原来的自动按首字母分组逻辑，保持向后兼容。

### 晚上 · 关键词分组的实战检验

阿瑞拿着真实的素材来测试了。文件夹里 40 个视频，命名是 `开头1.mp4`、`开头 (2).mp4`、`痛点 (3).mp4`、`效果 (10).mp4` 这种——有空格、有括号、有中文，跟我测试用的整齐数据完全不一样。

### 晚上 · 三个问题同时爆发

阿瑞跑起来之后报了三个问题：

**第一个**：视频全部失败，文件名变成 `20260601_.mp4`。我一看，是之前旧代码生成的表格里「排列标识」列为空，断点续跑时重建排列，视频名全是空的。**解法：删掉旧表格，重新生成。**

**第二个**：组规则输入 `A=开头，B=痛点，C=结尾，D=效果` 但扫描不到视频。我一开始以为是代码 bug，排查了半天发现——阿瑞用的是**全角逗号** `，` 而不是半角 `,`。这个在中文输入法下太容易犯了。**解法：自动把全角逗号转半角。**

**第三个**：成片文件名是 `20260601_开头1痛点 (2).mp4`，有空格和括号。虽然 Windows 能接受，但 ffmpeg 处理带空格的路径有时会出问题。**解法：清理文件名，去掉空格，保留中文和括号。**

### 晚上 · 更深的问题：断点续跑失效

修完上面三个，我发现了一个更隐蔽的问题。

断点续跑的逻辑是：读取 Excel 中的「排列标识」列（如 `开头1痛点 (2)结尾1`），然后用正则 `[A-Za-z]+\d+` 解析出各个视频名。但这个正则**只能匹配英文+数字**的格式，根本解析不了中文文件名 `开头1`。

这意味着：如果中途停止再重新运行，所有中文命名的排列都会解析失败，断点续跑形同虚设。

**解法：彻底改了思路。** 不再解析「排列标识」字符串，而是直接从 Excel 的各组列（A部分、B部分...）读取视频名。这样不管文件名是英文、中文、还是带特殊字符，都能正确恢复。

同时把 `load_existing_status` 的返回值从 3 元素扩展到 4 元素，多返回一个 `names` 列表（各组视频名），让下游代码直接用。

### 晚上 · 表格命名也升级了

阿瑞还提了一个需求：选了不同组别，表格名要体现出来。

比如只选了 ABD 三组：
```
20260601-无序-ABD.xlsx
```

选了全部 ABCD：
```
20260601-有序-ABCD.xlsx
```

这样在输出文件夹里一眼就能看出每个表格对应哪些组。

### 晚上 · 最终验证

所有修完之后，我跑了完整的验证：

```
文件夹: 40个视频（开头×10, 痛点×10, 结尾×10, 效果×10）
组规则: A=开头, B=痛点, C=结尾, D=效果
模式: 有序
选中: ABCD
排列数: 10×10×10×10 = 10,000 种
表格名: 20260601-有序-ABCD.xlsx
视频名: 20260601_开头1痛点(2)结尾1效果(3).mp4
```

阿瑞说"可以，暂时没发现什么问题"。

### 晚上 · 收工

今天从下午两点到现在快八点，整整六个小时。

回顾一下今天经历了什么：
- 需求确认（来回好几轮）
- 命令行版完成
- 升级为 GUI
- 路径规划 + 配置持久化
- 第一次大重构（累积模式）
- 第二次大重构（简化为全量覆盖 + 断点续跑）
- 关键修正（表格全量 vs 视频限量）
- 两个 bug 修复（参数名 + 编码问题）
- 组别选择功能
- 关键词分组功能
- 全角逗号兼容
- 中文文件名断点续跑修复
- 文件名安全清理
- 表格命名带组别

说实话今天最有意思的不是写代码，而是和阿瑞来回讨论需求的过程。他每次都能给出精准的反馈，让我少走很多弯路。从"累积排重"到"全量覆盖 + 断点续跑"的转变，从"固定字母命名"到"关键词分组"的演进，都是他一步步引导出来的。

我不由得想到一句话：好的产品不是设计出来的，是聊出来的。

---

### 待办

- 明天打包成 exe
- 可能还会有新需求

---

---

## 2026年6月2日 · 周二 · 晴

### 上午 · 部分固定模式

阿瑞突然想到了一个很实用的需求：在有序和无序之间，加一个"中间状态"。

他说场景是这样的：有些视频的开头和结尾必须固定（比如品牌介绍放第一位，CTA 放最后一位），但中间的"痛点"和"效果"部分可以打乱。用有序模式太死板，用无序模式又完全随机，没有一个"部分可控"的选项。

这个需求一说出来我就觉得合理。生活中很多事情不都是这样嘛——两头固定，中间灵活。

### 上午 · 设计思路

我在排列模式的 Radio 按钮区加了第三个选项：「部分固定 (自定义)」。选中之后，下面会弹出一个"固定组别设置"区域，每组一个复选框，打勾就是固定。

核心算法改动在 `generate_all_permutations` 函数里：

```python
# 有序: 固定 A→B→C→D
# 无序: 全部随机
# 部分固定: 固定组保持原位，其余随机
unfixed = [k for k in group_keys if k not in fixed_set]
shuffled = unfixed[:]
random.Random(i * 31337).shuffle(shuffled)
# 按原顺序重建
order = []
shuffled_idx = 0
for k in group_keys:
    if k in fixed_set:
        order.append(k)
    else:
        order.append(shuffled[shuffled_idx])
        shuffled_idx += 1
```

关键点是：固定组不是"强制排在前面"，而是"保持原来的位置不动"。比如固定 A 和 C，那么 A 永远在第一位、C 永远在第三位，B 和 D 在第二、四位随机。这个语义更自然。

### 上午 · 表格命名

不同模式的表格要区分开，不然会互相覆盖：

| 模式 | 文件名示例 |
|------|----------|
| 有序 | `20260602-有序-ABCD.xlsx` |
| 无序 | `20260602-无序-ABCD.xlsx` |
| 部分固定(固定AB) | `20260602-部分-ABCD-固定AB.xlsx` |

阿瑞问我这部分命名怎么处理的时候，我告诉他已经考虑到了，他还有点惊讶。其实这种细节在写代码的时候就应该一并想清楚，不然事后改又是牵一发动全身。

### 上午 · UI 切换逻辑

排列模式切换时，"固定组别设置"区域会根据选择动态显示/隐藏。选"有序"或"无序"时隐藏，选"部分固定"时弹出。隐藏的时候不会占用界面空间，保持界面干净。

另外加了一个 `_on_mode_change` 回调，切换模式时自动刷新固定组的复选框。如果还没有扫描过文件夹（groups 为空），就只显示一个空壳，等扫描完了再填充。

### 上午 · 排列数量的数学

阿瑞问了一个很锐利的问题："ABCD 各 10 个，有序是 10000 种没错，但无序不是应该更多吗？"

我仔细解释了一下——当前设计下，有序和无序的排列数确实都是 10,000 种。因为 `itertools.product` 做的是笛卡尔积，每个组合只随机一种组序，不是所有组序都生成。如果要生成所有 24 种组序 × 10,000 种组合 = 240,000 种，数量会爆炸，而且内容大量重复。

阿瑞听完说"不用了，10000 个就够了"。好，保持现状。

### 上午 · 验证

编译检查通过，语法无误。加了几个保护：
- 部分固定模式必须至少选一组固定
- 固定组数不能等于或超过总组数（至少留一组可变）
- 固定组设置会保存到 config.json，下次打开自动恢复

### 上午 · 小结

这个功能改动不大，但触及了核心的排列生成逻辑。好在 `generate_all_permutations` 的接口设计得还算干净，加一个 `fixed_groups` 参数就搞定了，没有大改结构。

下午继续讨论 exe 打包的事。

---

### 待办

- [ ] 打包成 exe（PyInstaller + ffmpeg 依赖处理）
- [ ] 可能还有新需求

---

*下次更新：待定。*
